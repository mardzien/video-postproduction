# Video Postproduction Dependencies
# ==================================

# Core dependencies
ffmpeg-python>=0.2.0    # FFmpeg bindings for video/audio processing
numpy>=2.0.0            # Numerical operations for audio buffers
scipy>=1.14.0           # WAV file I/O
Pillow>=10.0.0          # Image processing for text overlay
PyYAML>=6.0             # Template configuration files
mido>=1.3.0             # MIDI file parsing (external library, not our local mido module)
midi2audio>=0.1.1       # MIDI to audio rendering via FluidSynth

# Optional: pygame for mido audio playback
# pygame>=2.5.0         # Audio playback (not required for MIDI rendering)

# Development (optional)
# ruff>=0.4.0           # Linting
# mypy>=1.10.0          # Type checking
